
To build the input deck for program pg125 used in the book, type:

~/parafem/parafem/bin/p12meshgen pg125

at the command line.

Edit the directory path if you have installed ParaFEM elsewhere.

Note that the command assumes the file pg125.mg is in the working directory.
